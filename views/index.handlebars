<h2 class="text-center py-3">你想對誰講幹話</h2>
<div class="container">
  <form action="/" method="POST">
    <div class="row">
      {{#each job}}
      <label class="col-12 col-md-6  col-lg-4 mb-4">
        <div class="card">
          <img class="card-img-top" src="{{this.image}}" alt="{{this.career}}" style="height: 233.37px;">
          <div class="card-body d-flex justify-content-center align-items-center">
            <h5 class="card-title">{{this.career}}</h5>
          </div>
          <div class="card-footer  text-center">
            <label for="career">
              <input type="radio" name="career" id="{{this.career}}" value="{{this.career}}"
                {{#ifEquals req.target this.career}}checked{{/ifEquals}}>
              <!--這裡嘗試留住選過的 value但似乎沒辦法順利讓 ifEquals helper讀到每個職業的值-->
            </label>
          </div>
        </div>
      </label>
      {{/each}}
    </div>
    <div class="fixed-bottom float-right text-center">
      <button class="btn btn-warning py-2" type="submit"><i class="fas fa-poo"></i> 製造幹話</button>
      {{#if trashTalk}}
      <span class="bg-dark text-light py-3 px-4 rounded">{{trashTalk}}</span>
      {{/if}}
    </div>
  </form>
</div>